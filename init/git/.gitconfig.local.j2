[user]

  name = "{{ git_config_user_name | mandatory }}"
  email = "{{ git_config_user_email | mandatory }}"
  signingkey = ~/.ssh/id_ed25519.pub

[github]

  user = "{{ git_config_github_user | mandatory }}"
